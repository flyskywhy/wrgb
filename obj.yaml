# YOLOv5 🚀 by Ultralytics, AGPL-3.0 license
# Example usage: python train.py --data ../datasets/wrgb/obj.yaml --cfg ../datasets/wrgb/yolov5s.yaml
# parent
# ├── yolov5  ← git clone https://github.com/ultralytics/yolov5
# └── datasets
#     └── wrgb  ← git clone here


# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
path: ../datasets/wrgb  # dataset root dir
train: train.txt  # train images (relative to 'path')
val: train.txt  # val images (relative to 'path')
test:  # test images (optional)

# Classes
names:
  0: w
  1: r
  2: g
  3: b
